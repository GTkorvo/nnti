To develop this latex document, you first need to source the command
(in bash):

  $ . source_set_env

You can then make the PDF version of the document with:

  $ make

You can view the document on some systems with:

  $ xpdf ModelEvaluatorOverview.pdf

To quickly rebuild the document, just do:

  $ pdflatex ModelEvaluatorOverview

Back in xpdf you can then type 'R' and get a refreshed copy to look
at.
