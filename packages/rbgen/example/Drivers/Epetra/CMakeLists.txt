ADD_SUBDIRECTORY(Lena)
ADD_SUBDIRECTORY(Moon)
ADD_SUBDIRECTORY(SampleXML)

INCLUDE_DIRECTORIES(${RBGen_SOURCE_DIR}/src)
INCLUDE_DIRECTORIES(${RBGen_BINARY_DIR}/src)
INCLUDE_DIRECTORIES(${Teuchos_SOURCE_DIR}/src)
INCLUDE_DIRECTORIES(${Teuchos_BINARY_DIR}/src)
INCLUDE_DIRECTORIES(${Epetra_SOURCE_DIR}/src)
INCLUDE_DIRECTORIES(${Epetra_BINARY_DIR}/src)

#ADD_EXECUTABLE(RBGenDriver_EpetraMV ${RBGen_SOURCE_DIR}/src/RBGenDriver_EpetraMV.cpp)

#TARGET_LINK_LIBRARIES(RBGenDriver_EpetraMV rbgen teuchos epetra)

