PACKAGE_NAME=RBGen

#
# FRAMEWORK group
#

(FRAMEWORK, INSTALL, RBGenPyTests) {
    DIRS = ../python/test;
    ARGS = --testharness;
    EXIT-STATUS = STRING-COMPARISON;
    COMM = SERIAL, MPI(1-4);
}

(FRAMEWORK, INSTALL, RBGenPyExamples) {
    DIRS = ../python/example;
    ARGS = --testharness;
    COMM = SERIAL, MPI(1-4);
}
