

The following function generates n random numbers in [0,1]:

   void r8_rand(int n, r8 *v)

The initialization routine is

   void r8_rand_init(unsigned long seed);

and the current seed can be retrieved with

   unsigned long r8_rand_get_seed();


